import 'package:flutter/material.dart';

class SearchAndScreen extends StatelessWidget {
  const SearchAndScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SearchAndScreen'),
      ),
    );
  }
}
