import 'package:flutter/material.dart';

class TableroScreen extends StatelessWidget {
   
  const TableroScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('TableroScreen'),
      ),
    );
  }
}