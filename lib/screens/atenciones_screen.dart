import 'package:flutter/material.dart';

class AtencionesScreen extends StatelessWidget {
   
  const AtencionesScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('AtencionesScreen'),
      ),
    );
  }
}